# apb_spi_master_verilog
APB-Based SPI Master IP Core with modular Verilog RTL, APB register map implementation, configurable baud-rate generator, shift register, slave-select controller, and full waveform + synthesis reports.
